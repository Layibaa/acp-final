import express from "express";
// No authentication routes anymore
const router = express.Router();

// You can add other user-related routes here (without login/signup logic).

export default router;
